name: Uppdatera data

on:
  repository_dispatch:
    types: [update-data]

permissions:
  contents: write

# Två anrop tätt inpå varandra skulle annars starta parallella körningar
# som krockar vid push. Här köas de i stället, och en väntande äldre
# körning avbryts – varje payload innehåller hela listan, så den nyaste
# har alltid den mest kompletta bilden.
concurrency:
  group: update-data
  cancel-in-progress: true

jobs:
  skriv-data:
    runs-on: ubuntu-latest
    steps:
      - name: Hämta repot
        uses: actions/checkout@v4

      - name: Skriv data.json från payloaden
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const data = context.payload.client_payload.handelser || [];
            fs.writeFileSync('data.json', JSON.stringify(data, null, 2));

      - name: Committa och pusha
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data.json
          git diff --staged --quiet || git commit -m "Uppdaterar händelser"
          git push
